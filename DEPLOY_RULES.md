# スマート案内図エディタ デプロイ・バージョン管理・同期運用規約 (DEPLOY_RULES.md)

本ドキュメントは、「スマート案内図エディタ」（リポジトリ: `git@github.com:mdo3-system/map.git`、公開URL: `https://map.mdo3.com/`）における変更管理、バックアップ、Gitコミット、および本番Webサーバー（Xserver）へのデプロイに関する**絶対厳守の運用規約**を定めたものです。

---

## 1. 物理的バックアップ / Gitコミット および 本番サーバーへのデプロイの実行

いかなるコード修正・改訂を行う際も、必ず以下の手順を**一言一句違わず順番通りに実行**すること。

### ① 最新コードの取得（最優先）
作業着手前、またはGitコミットの直前には、**必ず最初にローカル環境で最新コードを取得**すること。
```bash
git pull origin main
```

### ② 物理バックアップの取得
Gitのみに依存せず、ローカル環境のファイル全体をスナップショットとして `_backups/` フォルダへ完全保全すること。
```bash
node scripts/backup.js [新バージョン] [作業内容識別子]
# 例: node scripts/backup.js v0.1.5 pre-deploy
```

### ③ 新バージョンの宣言と反映（バージョンファイル更新）
- **原則:** コードを修正する際は、**必ずパッチバージョンをインクリメント（`*.*.+1`）**すること。
  - トップ（`0.`）およびミドル（`.1`）の数字は、**管理者の明示的な指示がない限り絶対に変更してはならない**。
- `frontend/js/version.js` のコメントに更新履歴を追記し、`const APP_VERSION = "v*.*.*";` を更新すること。

### ④ Gitステージングおよびコミット
```bash
git add .
git commit -m "[コミットメッセージ (例: feat(v0.1.5): ...)]"
```
※バックアップ目的の場合は `git commit -m "Backup before: [指示内容]"` を使用。

### ⑤ GitHubへのプッシュ
```bash
git push origin main
```

### ⑥ 【重要】本番Webサーバー (Xserver) へのSSH自動デプロイ
GitHubへのプッシュ完了後、**必ず以下のSSHコマンドを実行し、本番Webサーバー（Xserver）へ変更を即座にデプロイ**すること。
```bash
ssh -o BatchMode=yes -p 10022 mdo3@mdo3.xsrv.jp "cd mdo3.com/public_html/map && git pull origin main"
```
- 本番Webサイトの公開URL: **`https://map.mdo3.com/`**
- Xserver上のドキュメントルート: `/home/mdo3/mdo3.com/public_html/map`

### ⑦ 【重要】データベース/マイグレーション実行規約（将来用）
将来的にデータベースの変更（ALTER TABLE等のマイグレーションスクリプト）やサーバー側初期化スクリプトを作成した場合は、ユーザーにブラウザからアクセスさせるのではなく、**AIが必ずSSH経由でコマンドラインからスクリプトを実行し、直接Xserver上で反映**させること。
```bash
ssh -o BatchMode=yes -p 10022 mdo3@mdo3.xsrv.jp "cd mdo3.com/public_html/map && php [スクリプト名].php"
```

### ⑧ 実行証拠のチャット欄への引用（義務）
「バックアップおよびデプロイ完了」とユーザーに報告する際は、**必ず実際に実行した Git/SSH コマンドの出力結果（ターミナルの生ログ）をチャット欄に引用**すること。証拠がない報告は虚偽と見なす。

---

## 2. 新バージョンの宣言と反映ルール (`version.js`)

本プロジェクトでは `frontend/js/version.js`（およびルート同期 `js/version.js`）を唯一のバージョン真実元（Single Source of Truth）とする。

```javascript
/**
 * スマート案内図エディタ バージョン一元管理ファイル
 * 
 * 【バージョン更新履歴】
 * - v0.1.12: 各種申請モード全面強化(A4/B5縦横・前後左右10mm余白、区画割ツール・角から何件目指定、建築士番号/氏名、記号/テキスト独立移動、案内図・申請図の完全作図データ分離)
 * - v0.1.11: 1/2500国土地理院白図による確認申請添付図面(付近見取図)作図機能(地名地番・住居表示表題欄・モード切替)新設
 * - v0.1.10: マニュアルページ(manual.html)の縦スクロールバグ修正(style.css依存解除とoverflow-y解放)
 * - v0.1.9: 実例「住ま居る」案内図に基づく完全図解マニュアル(manual.html / MANUAL.md)の再構築
 * - v0.1.8: 操作マニュアル(manual.html)のWeb公開およびヘッダー「📖 マニュアル」閲覧ボタン新設
 * - v0.1.7: Surface Pro 7および中画面幅(1280px〜1400px)向けヘッダーレイアウト最適化(画像保存ボタンの画面内収束対応)
 * - v0.1.6: map.mdo3.com直下URLアクセス対応(ルート直下配置)およびNginx配信最適化
 * - v0.1.5: Xserver本番同期規約(DEPLOY_RULES.md)策定、version.jsによるバージョン一元管理化、map.mdo3.comルート自動転送対応
 * - v0.1.4: 道路・線路の流心なぞり描き(トレース)の詳述、Xserver静的ホスティング対応(国土地理院API直叩きフォールバック)
 * - v0.1.3: マニュアル(MANUAL.md)作成、実画面キャプチャ配置
 * - v0.1.2: カギ型引き出し線両端ドラッグ、色ピッカー、駅名引き出し線、施設テキスト分離
 * - v0.1.1: 方位5種デザイン、施設記号スライダー、プロパティモーダル、GitHub連携
 */
const APP_VERSION = "v0.1.11";
```

- `frontend/index.html` の画面ヘッダーバッジおよび `state.js` 内の保存データは、すべて `APP_VERSION` から自動供給される。

---

## 3. 承認後の自律実行 (Autonomous Execution Post-Approval)

- ユーザーから実装計画への「GO」サイン（承認）が出た後は、ファイルの書き換えやターミナルコマンドの実行（`run_command`）の許可を毎回尋ねることなく、プロアクティブかつ自律的に実行すること。
- 最も重要なのは：
  1. **実行前にバックアップをとること（いつでも元に戻せる状態を作ること）**
  2. **実行前に計画を報告し承認を得ること**
- 承認後は「AIにすべて任された」と解釈し、迅速にタスクを完遂すること。
