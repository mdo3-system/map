# バージョン管理・バックアップ規定 (VERSION_MANAGEMENT.md)

## 1. バージョン命名規則と厳格ルール
本プロジェクトは、`v0.1.1` よりバージョン管理を開始し、**現在バージョンは `v0.1.2`** です。

### 🚨 厳守ルール（AIおよび全開発者共通）
1. **パッチ番号（末尾の数字）の自動インクリメント**:
   - 以降、いかなる**些細な変更・修正・機能追加であっても、必ず末尾のバージョン番号を +1** してください。
   - 例: `v0.1.1` → `v0.1.2` → `v0.1.3` ...
2. **メジャー・マイナー番号（トップ・ミドル数字）の変更禁止**:
   - トップ（メジャー: `0`）およびミドル（マイナー: `1`）の数字は、**絶対に勝手に変更してはいけません**。
   - **「この数字の変更は管理者の指示による以外は変更しない」** ことを厳格に遵守します。

---

## 2. Git操作と自動バックアップ規定
誤操作やコンフリクト等によるコード消失を防ぎ、いつでも過去の任意時点に即座に復元できるよう、以下の規定を定めます。

1. **GitPush / GitPull 前の完全バックアップ必須**:
   - リモートからの Pull、およびリモートへの Push を実行する直前に、必ずローカル完全バックアップを取得します。
   - バックアップ実行コマンド:
     ```bash
     node scripts/backup.js <現在のバージョン> <push/pull>
     ```
2. **バックアップの保存場所**:
   - プロジェクト直下の `_backups/` フォルダ配下に、バージョン番号・操作種別・タイムスタンプ付きで自動アーカイブされます。
   - 例: `_backups/v0.1.1_pre-push_20260923_173630/`
3. **復元（ロールバック）手順**:
   - 何らかの理由で直前の状態や過去バージョンへ戻す場合は、該当する `_backups/<バージョン_日時>/` ディレクトリ内のファイルをカレントディレクトリへ上書きコピーすることで瞬時に復元可能です。

---

## 3. リモートリポジトリ情報
- **リモートURL**: `git@github.com:mdo3-system/map.git`
- **追跡ブランチ**: `main`
